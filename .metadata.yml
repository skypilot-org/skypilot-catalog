# Mapping between version and SkyPilot commit. The CI will update the catalogs
# for all versions listed here. We keep older versions for backward
# compatibility.
version_commit:
  v9: latest
  # v8 and v7 are pinned to fixed commits (the last one before their successor's
  # schema change), so they keep their schema; any newer commit would fetch v9
  # catalogs.
  v8: f4ad9ecface7a6308cd23ba154790f3c9a6a4916
  v7: 6a9fed7b515c4c7dc0942da1c35c15f430a14e8e
